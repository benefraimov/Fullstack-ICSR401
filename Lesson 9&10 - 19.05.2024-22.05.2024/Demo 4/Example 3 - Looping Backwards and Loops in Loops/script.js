// Looping Backwards and Loops in Loops
// 0, 1, 2, 3, 4
// 4, 3, 2, 1, 0
// show loop in loop example

let arr = [
    "a",
    "b",
    "c",
];

for(let i = (arr.length -1); i >= 0;i--){
    //console.log(i);
    console.log(arr[i]);
}